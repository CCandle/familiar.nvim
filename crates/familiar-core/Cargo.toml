[package]
name = "familiar-core"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Lifecycle-bound Rust sidecar for familiar.nvim"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
